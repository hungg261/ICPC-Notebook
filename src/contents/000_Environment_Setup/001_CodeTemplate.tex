% Template \& Fast I/O

Compile command:
\begin{lstlisting}[language=bash, backgroundcolor=\color{gray!6}]
g++ -DLOCAL -O3 -std=c++14 main.cpp -o main
\end{lstlisting}

\begin{lstlisting}
#include <bits/stdc++.h>
using namespace std;

void solve() {}

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);
    int t = 1; cin >> t;
    while (t--) solve();
    return 0;
}
\end{lstlisting}